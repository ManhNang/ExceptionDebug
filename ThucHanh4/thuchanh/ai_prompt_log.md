Vẽ cây phân cấp của Throwable trong Java và giải thích sự khác biệt về mặt bắt buộc xử lý giữa Exception và RuntimeException tại thời điểm biên dịch

Việc lạm dụng Checked Exception cho mọi loại lỗi dẫn đến các khối try-catch rỗng hoặc lồng nhau (Arrow Anti-pattern) gây rác mã nguồn như thế nào?

Dựa trên tiêu chí nào để quyết định một ngoại lệ nên là Checked (cần phục hồi) hay Unchecked (do sai logic/dữ liệu đầu vào)?

Giải thích triết lý 'Fail-Fast' trong lập trình và tại sao Unchecked Exception lại là công cụ hoàn hảo để triển khai nguyên lý này nhằm chặn lỗi lan rộng?

Quy trình từng bước để chuyển đổi một hệ thống lạm dụng Checked Exception sang mô hình Clean Code sử dụng Unchecked Exception kết hợp với Global Exception Handler là gì?
